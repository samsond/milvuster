[package]
name = "milvuster"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.12.3"
prost = "0.13.4"
tokio = {version = "1.43.0", features = ["full"]}

[build-dependencies]
tonic-build = "0.12.3"

[features]
generate-protos = []
